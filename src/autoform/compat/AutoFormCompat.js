import React from 'react';
import PropTypes from 'prop-types';

export default class AutoFormCompat extends React.Component {
    static propTypes = {
        schema: PropTypes.object,
        uiAdapter: PropTypes.object4
    };

    render() {
        return (
            false
        )
    }
}